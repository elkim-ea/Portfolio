# 🌱 Frontend - Setup Guide

## 1. 요구사항

* **Node.js**: v22.x 이상
* **npm**: v10.x 이상

  > 버전 확인:
  >
  > ```bash
  > node -v
  > npm -v
  > ```

---

## 2. 프로젝트 설치

```bash
# 패키지 의존성 설치
npm install
```

---

## 3. 실행

```bash
# 개발 서버 실행 (http://localhost:5173)
npm start
# 또는
npm run dev
```

---

## 4. 주요 라이브러리

* **React 18**: UI 라이브러리
* **Vite**: 빌드 툴
* **React Router**: 라우팅
* **Axios**: API 통신
* **React Hook Form**: 폼 입력 및 유효성 검사 관리
* **React Query (TanStack Query)**: 서버 데이터 캐싱 및 상태 관리
* **React Query Devtools**: React Query 개발자 도구
* **Chart.js + react-chartjs-2**: 대시보드 시각화
* **dayjs**: 날짜/시간 계산 및 포맷팅

---

## 5. 폴더 구조

```
frontend/
 ├─ public/          # 정적 파일
 ├─ src/
 │   ├─ api/         # API 요청 모듈
 │   ├─ assets/      # 이미지, 아이콘
 │   ├─ components/  # 재사용 컴포넌트 (버튼, 카드, 레이아웃 등)
 │   ├─ hook/        # 커스텀 훅
 │   ├─ pages/       # 페이지 단위 컴포넌트 (Home, Quest, Company 등)
 │   ├─ router/      # 라우터 설정
 │   ├─ styles/      # 전역 스타일 (CSS)
 │   ├─ utils/       # 유틸 함수
 │   ├─ App.css
 │   └─ App.tsx
 └─ package.json
```

---

## 6. 실행 흐름

1. `src/main.tsx` → 애플리케이션 진입점
2. `App.tsx` → 전역 레이아웃 및 라우팅 진입
3. `router/` → 페이지 경로 관리
4. `pages/` → 주요 화면(Home, Quest, Company 등)
5. `components/` → 재사용 UI 조립
6. `api/` → Axios 기반 API 호출 관리
