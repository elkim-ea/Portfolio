apiVersion: v1
kind: Service
metadata:
  name: matcha-backend-service
spec:
  type: LoadBalancer
  selector:
    app: matcha-backend
  ports:
    - protocol: TCP
      port: 8080        # external port 그대로 8080 사용
      targetPort: 8080  # Pod 내부 포트
