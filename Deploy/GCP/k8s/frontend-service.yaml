apiVersion: v1
kind: Service
metadata:
  name: matcha-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: matcha-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
